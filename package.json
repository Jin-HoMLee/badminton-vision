{
  "name": "badminton-vision-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first, confidence-aware badminton rally review overlay.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs test/*.test.js analysis/test/*.test.js",
    "runtime-smoke": "npm run build && node --test test/runtime-integration.test.js test/capture-backpressure.test.js",
    "check": "npm run build && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
