# Repository-level no-mistakes configuration.
# Phase 1 CI: Build validation and extension manifest checks
# GitHub Actions CI workflow is now active; checks are enforced.

gates:
  - build    # npm run build
  - test     # npm run test + npm run validate-extension
