Keyboard glyph. Labeling is keyboard-first (`S` `E` `1–9` `O` `Esc` `Enter`), so shortcuts are always visible next to their action.

```jsx
<span>Accept suggestion <KeyHint tone="accent">Enter</KeyHint></span>
```
