Selectable pill for filters and multi-select sets (shot-family filters on the summary, rally tags).

```jsx
<Chip selected count={12}>Smash</Chip>
```

Selected = lime tint + lime text. For mutually exclusive choices use SegmentedControl instead.
