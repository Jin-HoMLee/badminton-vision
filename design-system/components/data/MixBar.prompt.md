Proportion bar for shot mix and winner/error mix.

```jsx
<MixBar segments={[{label:"Clear",value:22,color:"var(--player-a)"},{label:"Unclassified",value:6}]} />
```

Always include the unclassified/unknown share as its own slate segment — never normalise it away.
