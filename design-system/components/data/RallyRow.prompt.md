A ranked rally on the match summary.

```jsx
<RallyRow rank={1} rallyId={23} index={87} shots={31} duration="42.6s" outcome="winner" timestamp="18:42" partial />
```

The timestamp button copies/reveals the time; it must not call `currentTime`. `partial` prints the `*` that the index footnote explains.
