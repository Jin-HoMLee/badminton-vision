A single statistic in mono numerals with an uppercase micro-label.

```jsx
<StatTile label="Avg rally" value="8.4" unit="shots" note="42 rallies" />
```

Use `note` for sample size or partial-data caveats; the PRD requires sample size wherever a derived number is shown.
